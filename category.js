const params=new URLSearchParams(location.search);const key=params.get('cat')||'banks';const cat=window.APP_DIRECTORY[key]||window.APP_DIRECTORY.banks;document.title=`${cat.title} | iStoreNavid`;document.getElementById('catTitle').textContent=cat.title;document.getElementById('catEmoji').textContent=cat.emoji;const grid=document.getElementById('appsGrid');cat.apps.forEach(app=>{const[name,desc,url,status='نسخه وب رسمی',logo]=app;const card=document.createElement('article');card.className='app-card';const image=document.createElement('img');image.className='official-app-logo';image.src=logo;image.alt=`لوگوی ${name}`;image.loading='lazy';image.referrerPolicy='no-referrer';const content=document.createElement('div');content.innerHTML=`<div class="app-title-row"><h2>${name}</h2><span class="web-badge">${status}</span></div><p>${desc}</p><a class="open-app" href="${url}" target="_blank" rel="noopener noreferrer">باز کردن نسخه وب</a><button class="guide-mini" type="button">راهنمای نصب</button>`;content.querySelector('.guide-mini').onclick=()=>alert('۱) وب‌اپ را در Safari باز کنید.\n۲) دکمه Share را بزنید.\n۳) Add to Home Screen را انتخاب کنید.\n۴) روی Add بزنید.');card.append(image,content);grid.appendChild(card);});